<a class="max-w rounded overflow-hidden shadow-lg border border-gray-200 bg-white dark:bg-gray-200 flex flex-col" href="{{talk.data.page.url}}">
  <div class="px-6 py-4 flex-grow">
    <div class="font-bold text-xl my-2">{{ talk.data.talk.title }}</div>
  </div>
  <div class="flex items-center px-6 pb-6">
    {% for speaker in talk.data.speakers %}
      <img class="w-10 h-10 rounded-full mr-4" src="/assets/img/people/{{speaker.photo}}" alt="Photo of {{speaker.name}}">
    {% endfor %}
    <div class="text-sm">
      {% assign names = talk.data.speakers | map: "name" %}
      {% assign twitters = '' %}
      {% for speaker in talk.data.speakers %}
        {% if speaker.twitter %}
          {% if twitters.size > 0 %} {% assign twitters = twitters | append: ',' %} {% endif %}
          {% assign twitters = twitters | append: '@' %}
          {% assign twitters = twitters | append: speaker.twitter %}
        {% endif %}
      {% endfor %}

      <p class="text-gray-900 leading-none">{{names | join: " & "}}</p>
      {% if twitters.size > 0 %}
        <p class="text-gray-700">{{twitters | split: ',' | join: " & " }}</p>
      {% endif %}

      {% comment %} {% if talk.data.speaker.twitter %}
      {% endif %} {% endcomment %}
    </div>
  </div>
</a>
